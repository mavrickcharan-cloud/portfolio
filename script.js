const loader=document.getElementById('loader');window.addEventListener('load',()=>setTimeout(()=>loader.classList.add('hide'),650));
const glow=document.getElementById('cursorGlow');window.addEventListener('pointermove',e=>{glow.style.opacity='1';glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'});
const words=['AI Developer','Python Developer','Cybersecurity Learner','Web Developer'];let wi=0,ci=0,del=false;const typing=document.getElementById('typing');function type(){const word=words[wi];typing.textContent=word.slice(0,ci);if(!del&&ci<word.length)ci++;else if(del&&ci>0)ci--;else if(!del){del=true;return setTimeout(type,1200)}else{del=false;wi=(wi+1)%words.length}setTimeout(type,del?45:80)}type();
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(!entry.isIntersecting)return;entry.target.classList.add('visible');observer.unobserve(entry.target)}),{threshold:.14});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const skills=document.querySelector('.skills-console');if(skills){const skillsObserver=new IntersectionObserver(([entry])=>{if(!entry.isIntersecting)return;skills.querySelectorAll('.skill-fill').forEach(el=>el.style.width=el.dataset.width+'%');skills.querySelectorAll('.meter-value').forEach(el=>{const target=+el.dataset.value,start=performance.now();const tick=now=>{const p=Math.min((now-start)/1100,1);el.textContent=Math.round(target*(1-Math.pow(1-p,3)));if(p<1)requestAnimationFrame(tick)};requestAnimationFrame(tick)});skillsObserver.disconnect()},{threshold:.3});skillsObserver.observe(skills)}
const filters=document.querySelectorAll('.project-filter'),cards=document.querySelectorAll('.project-card');filters.forEach(btn=>btn.addEventListener('click',()=>{filters.forEach(x=>x.classList.toggle('active',x===btn));cards.forEach(card=>card.classList.toggle('hidden',btn.dataset.filter!=='all'&&card.dataset.category!==btn.dataset.filter))}));
const links=document.querySelectorAll('.nav-links a'),sections=[...document.querySelectorAll('main section[id]')];const navObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)links.forEach(link=>link.classList.toggle('active',link.getAttribute('href')==='#'+entry.target.id))}),{rootMargin:'-35% 0px -55%'});sections.forEach(el=>navObserver.observe(el));
const menu=document.getElementById('menuToggle'),nav=document.getElementById('navLinks');menu.addEventListener('click',()=>nav.classList.toggle('open'));links.forEach(l=>l.addEventListener('click',()=>nav.classList.remove('open')));document.getElementById('toTop').addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));document.getElementById('year').textContent=new Date().getFullYear();
